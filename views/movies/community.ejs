<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Community Movies - Reel Keeper</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <h1>Community Movies</h1>
    <nav>
      <a href="/">Dashboard</a>
      <a href="/movies/new">New Movie</a>
      <a href="/movies/community">Community</a>
      <form action="/users/logout" method="POST" style="display:inline">
        <button type="submit">Logout</button>
      </form>
    </nav>
  </header>

 <main>
  <% Object.keys(moviesByUser).forEach(username => { %>
  <h2><%= username %>’s Watchlist:</h2>
  <ul>
    <% moviesByUser[username].forEach(movie => { %>
      <li>
        <strong><%= movie.title %></strong> (<%= movie.year %>)<br>
        <% if (movie.imageUrl) { %>
          <img src="<%= movie.imageUrl %>" alt="<%= movie.title %> poster" style="max-width:150px;">
        <% } else { %>
          <em>[No poster available]</em>
        <% } %>
      </li>
    <% }) %>
  </ul>
<% }) %>

</main>


</body>
</html>
