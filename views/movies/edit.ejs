<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit <%= movie.title %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <main>
    <h1>Edit Movie</h1>
    <form action="/movies/<%= movie._id %>?_method=PUT" method="POST">
      <label>Title</label>
      <input type="text" name="title" value="<%= movie.title %>" required><br>
      <label>Year</label>
      <input type="number" name="year" value="<%= movie.year %>"><br>
      <label>Rating</label>
      <input type="number" name="rating" min="0" max="5" step="0.1" value="<%= movie.rating %>"><br>
      <button type="submit">Update Movie</button>
    </form>
    <a href="/movies/<%= movie._id %>">Back to Movie</a>
  </main>
</body>
</html>
