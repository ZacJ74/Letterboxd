<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Movie</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container mx-auto max-w-2xl px-4 py-8">
        <header class="mb-8 flex justify-between items-center">
            <h1 class="text-3xl font-bold">Add a New Movie</h1>
            <a href="/" class="button">Back to Dashboard</a>
        </header>
        
        <form action="/movies" method="POST" class="bg-white p-6 rounded-lg shadow-md">
            
            <!-- EJS conditional to display validation errors -->
            <% if (error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
            <% } %>

            <div class="form-group">
                <label for="title" class="form-label">Title:</label>
                <input type="text" id="title" name="title" required class="form-input">
            </div>
            
            <div class="form-group">
                <label for="year" class="form-label">Year:</label>
                <input type="number" id="year" name="year" min="1888" max="<%= new Date().getFullYear() %>" class="form-input">
            </div>
            
            <div class="form-group">
                <label for="review" class="form-label">Review:</label>
                <textarea id="review" name="review" rows="4" class="form-input"></textarea>
            </div>
            
            <div class="form-group">
                <label for="rating" class="form-label">Rating (1-5):</label>
                <input type="number" id="rating" name="rating" min="1" max="5" class="form-input">
            </div>

            <div class="form-group">
                <label for="imageUrl" class="form-label">Image URL (optional):</label>
                <input type="url" id="imageUrl" name="imageUrl" class="form-input">
            </div>
            
            <button type="submit" class="button button-primary mt-4 w-full">Add Movie</button>
        </form>
    </div>
</body>
</html>
