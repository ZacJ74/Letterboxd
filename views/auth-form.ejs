<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Reel Keeper</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <main>
    <h1><%= title %></h1>
    <% if (message) { %><p style="color:red"><%= message %></p><% } %>

    <form action="/users/<%= mode %>" method="POST">
      <label>Username</label>
      <input type="text" name="username" required><br>
      <label>Password</label>
      <input type="password" name="password" required><br>
      <button type="submit"><%= title %></button>
    </form>

    <% if (mode === 'signup') { %>
      <p>Already have an account? <a href="/users/auth?mode=login">Login</a></p>
    <% } else { %>
      <p>Need an account? <a href="/users/auth?mode=signup">Sign Up</a></p>
    <% } %>
  </main>
</body>
</html>

