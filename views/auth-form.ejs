<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('partials/nav') %>

  <div class="auth-container">
       <h1>
      <% if (mode === 'login') { %>
        Welcome back to ReelKeeper
      <% } else { %>
        Welcome to ReelKeeper
      <% } %>
    </h1>

    <% if (message) { %>
      <p class="error"><%= message %></p>
    <% } %>

    <form action="/users/<%= mode %>" method="POST">
      <div>
        <label for="username">Username</label>
        <input type="text" id="username" name="username" required>
      </div>

      <div>
        <label for="password">Password</label>
        <input type="password" id="password" name="password" required>
      </div>

      <button type="submit" class="btn">
        <%= mode === 'login' ? 'Login' : 'Sign Up' %>
      </button>
    </form>

    <p class="auth-switch">
      <% if (mode === 'login') { %>
        Don’t have an account? <a href="/users/auth?mode=signup">Sign up here</a>.
      <% } else { %>
        Already have an account? <a href="/users/auth?mode=login">Login here</a>.
      <% } %>
    </p>
  </div>
</body>
</html>
